<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Symfony App{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">
    <nav class="bg-blue-600 text-white py-4 shadow-lg">
        <div class="container mx-auto flex justify-between items-center">
            <a href="{{ path('niveaux_list') }}" class="text-2xl font-bold hover:text-gray-200">Symfony App</a>
            <ul class="flex space-x-6">
                <li><a href="{{ path('professeurs_list') }}" class="hover:text-gray-200">Professeurs</a></li>
                <li><a href="{{ path('niveaux_list') }}" class="hover:text-gray-200">Niveaux</a></li>
                <li><a href="{{ path('classes_list') }}" class="hover:text-gray-200">Classes</a></li>
            </ul>
        </div>
    </nav>
    <main class="container mx-auto mt-10">
        {% block body %}{% endblock %}
    </main>
</body>
</html>